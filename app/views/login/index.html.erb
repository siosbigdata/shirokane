<%= render 'common/header' %>
<div id="login">
	<%= form_tag :controller => 'login', :action => 'create' do -%>
	<h2 id="login_title"><%= t('login') %></h2>
	<table>
	<tbody>
		<tr>
			<th><%= label_tag :mail, t('mail') %></th>
			<td><%= text_field_tag :mail, params[:mail] %></td>
		</tr>
		<tr>
			<th><%= label_tag :pass, t('password') %></th>
			<td><%= password_field_tag :pass , params[:pass]%></td>
		</tr>
		<% if @errormsg %>
			<tr><td colspan="2">
			<div id="error_msg"><%= t('error_login_message') %><br /></div>
			</td></tr>
		<% end %>
	</tbody>
	</table>
	<div id="login_action"><%= submit_tag t('login') %></div>
	<% end %>
	
	<div id="forgotten_password"><%= link_to t('forgotten_password'), new_password_reset_path %></div>
</div>
<%= render 'common/footer' %>
