<%= render 'admin/common/header' %>
<%= render 'admin/common/sidebar' %>
<!-- administrator main content start -->
<div id="content">
	<h1><%= t('menu_group') %></h1>
	<h2><%= t('title_group_graph_select') %></h2>


<table class="coder">
  <thead>
    <tr>
      <th><%= t('title_select') %></th>
      <th><%= t('title_graph') %></th>
    </tr>
  </thead>

  <tbody>
  	<%= form_tag('/admin/groups/setgraph') do |f| %>
    <% @graph.each do |g| %>
      <tr>
        <td><%= check_box 'graph', 'a',{:checked => true}, g.id, false %></td>
        <td><%= g.title %></td>
      </tr>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('title_back'), admin_groups_path %>

</div><!-- administrator main content end -->
<%= render 'admin/common/footer' %>